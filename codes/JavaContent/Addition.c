#include<stdio.h>
int main(){
    int a,b;
    printf("Enter the values of a and b ");
    scanf("%d%d",&a,&b);
    int sum=a+b;
    printf("The addition of two numbers is %d ", sum);

    return 0;
}